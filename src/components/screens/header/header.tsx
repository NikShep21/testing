import styles from './header.module.scss'

const header = () => {
  return (
    <div className={styles.header}>
        <h1 className={styles.headerName}>TESTTEST</h1>
    </div>
  )
}

export default header